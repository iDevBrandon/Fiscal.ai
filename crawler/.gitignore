# Secrets — never commit
.env
.env.*

# Node
node_modules/
dist/
*.tsbuildinfo

# Downloaded PDFs + parsed page text (local cache, can be re-created)
cache/

# Debug dumps
dump-*.txt

# Crawl output (keep the dir, ignore generated files)
output/*.json
!output/.gitkeep

